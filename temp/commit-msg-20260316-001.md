feat(search): add exact filename match boost for stemmer-conflated queries

Add `apply_filename_boost` post-processing step that promotes files whose
basename exactly matches the query (or whose basename starts with the query
followed by `.`). Guards against conflicting with the directory boost by
only activating when the query contains a `.`. Refactors
`find_matching_dirs` into `extract_top_level_dir` + `dir_matches_tokens`
helpers to satisfy linter length limits.
