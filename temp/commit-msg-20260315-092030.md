feat: expand file-type penalties for infrastructure and config files

Add new penalty predicates for build output, IDE configs, lockfiles, Dockerfiles,
dot-config files, migrations, and type declarations. Update type_penalty() with
new penalty tiers to ensure source files rank higher than infrastructure/config files.

- Tier 1.0: build_output/generated (bin/dist output)
- Tier 0.9: ide_config/lockfile (vscode settings, pnpm-lock.yaml)
- Tier 0.8: snapshot (jest snapshots)
- Tier 0.5: test (.test.ts/.spec.ts)
- Tier 0.4: dockerfile (infrastructure)
- Tier 0.3: story (.stories.tsx)
- Tier 0.2: type_declaration (.d.ts), migration, styled
- Tier 0.15: dot_config (.prettierrc, .npmrc)
- Tier 0.1: barrel/index files

All 13 tests pass.
